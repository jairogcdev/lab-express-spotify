<div>
  {{#each dataArtist}}
    <p>{{this.name}}</p>
    {{#each this.images}}
      {{#if this.height}}
        <img src="{{this.url}}" alt="image" />
      {{/if}}
    {{/each}}
    <button><a href="/albums/{{this.id}}">View Albums</a>
    </button>
  {{/each}}
  <button><a href="/">Go home</a></button>
</div>